services:
  server123-landing:
    image: {{ image }}
    build: .
    ports:
      - "3000"

    restart: unless-stopped
    networks:
      - shared

networks:
  shared:
    name: shared
    external: true
